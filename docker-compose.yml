services:
  datey:
    build: .
    container_name: datey
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - datey_data:/data
    environment:
      - PORT=8080
      - DATA_DIR=/data
      - SCHEDULER_HOUR=8
      - REMINDER_DAYS=7
      # Email
      - SMTP_HOST=
      - SMTP_PORT=587
      - SMTP_USER=
      - SMTP_PASS=
      - SMTP_TLS=true
      - NOTIFICATION_EMAIL=
      # Gotify
      - GOTIFY_URL=
      - GOTIFY_TOKEN=
      # Telegram
      - TELEGRAM_BOT_TOKEN=
      - TELEGRAM_CHAT_ID=

volumes:
  datey_data:
