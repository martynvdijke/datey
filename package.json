{
  "name": "datey",
  "version": "1.3.0",
  "private": true,
  "description": "Relationship event reminder application",
  "scripts": {
    "build:ts": "echo 'No TypeScript build needed'"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
